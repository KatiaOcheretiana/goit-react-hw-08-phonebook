import { Box } from '@mui/material';
import styled from 'styled-components';

export const FindBox = styled(Box)({
  margin: ' 14px 0',
  paddingLeft: '10px',
  fontSize: ' 14px',
  fontWeight: '500',
});
